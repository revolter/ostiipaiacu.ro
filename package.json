{
    "scripts": {
        "serve": "watchy --watch '_config.yml' --watch '_plugins/*.rb' -- bundle exec jekyll serve --trace --baseurl '/ostiipaiacu.ro'"
    },
    "devDependencies": {
        "watchy": "*"
    }
}
